#include <world.h>

World::World()
	: time(0), player(), area(), terrain()
{}

void World::tick(){
  
  
  /* Update loop */
  //vector<Displayable>::iterator it;
  //it = objects.begin();

  time++;
}

